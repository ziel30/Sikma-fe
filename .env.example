# Base URL of the SIKMA backend API.
# Also used as the Socket.IO host — the client connects to the /match namespace on it.
# Must match the port your backend is running on (the API defaults to 3000, which
# collides with Next.js, so run it on another port such as 8080).
NEXT_PUBLIC_API_URL=http://localhost:8080
