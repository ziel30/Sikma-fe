/** Name of the session cookie checked by middleware + server requests. */
export const SESSION_COOKIE = "sikma_session";

/** localStorage key holding the bearer token for the browser Axios client. */
export const SESSION_TOKEN_KEY = "sikma:token";

/** localStorage key holding the signed-in user's role (0 user, 1 admin, 2 super). */
export const SESSION_ROLE_KEY = "sikma:role";
